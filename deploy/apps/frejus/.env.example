# Fichier .env de /opt/apps/frejus sur le VPS. Copier en `.env`, remplir, puis
# `chmod 600 .env`. NE JAMAIS COMMITER le fichier rempli.
#
# Générer les secrets sur le VPS :
#   openssl rand -base64 18   (mots de passe)
#   openssl rand -base64 48   (JWT_SECRET)

# --- Point d'entrée public ---
# Lus par nginx-apply.sh (deploy/reference/nginx/) si tu passes par ce script.
#
# Mode DOMAINE + HTTPS (frejus.conf.template) : trois enregistrements DNS A vers
# l'IP du VPS, un vhost HTTPS par domaine, certificats Let's Encrypt.
#
# Mode IP NUE (frejus-ip.conf.template) : pas de domaine, pas de HTTPS. Les trois
# variables reçoivent alors la même valeur — l'IP publique du VPS — et le routage se
# fait par chemin et par port :
#   http://IP/        site      http://IP/api/  API      http://IP:8080/  admin
# Passer d'un mode à l'autre = changer NGINX_TEMPLATE ci-dessous, relancer
# nginx-apply.sh, et relancer le pipeline CI/CD pour frontend et admin (les URL VITE_* sont
# gravées dans les bundles au build).
SITE_DOMAIN=pixellia.fr
API_DOMAIN=api.pixellia.fr
ADMIN_DOMAIN=admin.pixellia.fr
# Adresse utilisée par Let's Encrypt pour les alertes d'expiration (mode domaine).
ACME_EMAIL=contact@pixellia.fr

# Template Nginx appliqué : frejus.conf.template (domaine + HTTPS, défaut) ou
# frejus-ip.conf.template (IP nue, HTTP seul).
NGINX_TEMPLATE=frejus.conf.template
# Port d'écoute du panneau admin en mode IP nue uniquement. A ouvrir au pare-feu :
# `sudo ufw allow 8080/tcp`. Ignoré en mode domaine (l'admin est un vhost sur 443).
ADMIN_PORT=8080

# --- Images Docker (réécrites à chaque déploiement par deploy.sh) ---
FRONTEND_IMAGE=ghcr.io/kevinvoli/frejus-frontend:latest
BACKEND_IMAGE=ghcr.io/kevinvoli/frejus-backend:latest
ADMIN_IMAGE=ghcr.io/kevinvoli/frejus-admin:latest

# --- API ---
# Origines autorisées à appeler l'API, séparées par des virgules.
# Ne JAMAIS laisser "*" en production : l'admin s'authentifie en JWT.
# En mode IP nue : http://<IP>,http://<IP>:8080 (le site est en même origine que
# l'API, l'admin est sur un autre port et lui est donc cross-origin).
CORS_ORIGIN=https://pixellia.fr,https://admin.pixellia.fr

# --- Base de données ---
# MySQL est administré à la main, hors de ce dépôt : le déploiement ne le crée pas,
# ne le démarre pas et ne le reconfigure pas. Il faut donc lui indiquer où joindre la
# base, et sur quel réseau Docker l'API doit être branchée pour y arriver.
#
#   Base dans un conteneur Docker (le cas courant)
#     DB_NETWORK = nom du réseau Docker sur lequel ce conteneur est branché
#                  (`docker inspect -f '{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}' <conteneur>`)
#     DB_HOST    = nom ou alias du conteneur MySQL sur ce réseau
#
#   Base installée sur l'hôte (hors Docker)
#     DB_NETWORK=bridge          (le réseau par défaut, toujours présent)
#     DB_HOST=172.17.0.1         (adresse de l'hôte vue depuis ce réseau ;
#                                 vérifier avec `ip -4 addr show docker0`)
#     MySQL doit alors écouter sur cette interface, pas seulement sur 127.0.0.1.
#
#   Base sur un autre serveur
#     DB_NETWORK=bridge
#     DB_HOST=<ip-ou-hostname>
#
# L'utilisateur et la base doivent exister au préalable, avec des droits limités à
# cette seule base.
DB_NETWORK=bridge
DB_HOST=172.17.0.1
DB_PORT=3306
DB_USERNAME=frejus
DB_PASSWORD=CHANGE-ME
DB_DATABASE=frejus
# true tant qu'il n'y a pas de migrations TypeORM : les tables sont créées depuis
# les entités au démarrage. Voir docs/ANALYSE-PLAN-BACKEND.md avant de passer à false.
DB_SYNCHRONIZE=true

# --- Authentification admin ---
JWT_SECRET=CHANGE-ME
JWT_EXPIRES_IN=12h

# Compte admin créé au premier démarrage si la table admin_users est vide.
# Changer le mot de passe depuis le panneau juste après le premier login.
ADMIN_EMAIL=admin@pixellia.fr
ADMIN_PASSWORD=CHANGE-ME
